{
  "image": "ghcr.io/tdrwenski/quandary:latest",
  "hostRequirements": {
    "cpus": 4
  },
  "waitFor": "postCreateCommand",
  "onCreateCommand": "git submodule update --init --recursive && . /workspaces/quandary/.ci-scripts/mpi_wrapper.sh",
  "postCreateCommand": "source ~/.bashrc && python -c 'import jupyter_client; print(f\"Kernel ready: {jupyter_client.kernelspec.find_kernel_specs().get(\"quandary-env\", \"Kernel not found\")}\")'  && echo 'MPI check:' && which mpirun && mpirun --version",
  "customizations": {
    "codespaces": {
      "openFiles": [
        "examples/pythoninterface/QuandaryWithPython_HowTo.ipynb"
      ]
    },
    "vscode": {
      "extensions": [
        "ms-toolsai.jupyter",
        "ms-python.python"
      ]
    }
  }
}
